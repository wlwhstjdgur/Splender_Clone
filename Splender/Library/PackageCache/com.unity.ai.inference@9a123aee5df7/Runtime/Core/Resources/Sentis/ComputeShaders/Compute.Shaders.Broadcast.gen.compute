// This is auto-generated -- do not modify directly


#pragma kernel ScalarBroadcastPRelu SCALARBROADCAST PRELU
#pragma kernel BroadcastPRelu BROADCAST PRELU
#pragma kernel ElementwisePRelu ELEMENTWISE PRELU
#pragma kernel ScalarBroadcastPowFloatFloat SCALARBROADCAST POWFLOATFLOAT
#pragma kernel BroadcastPowFloatFloat BROADCAST POWFLOATFLOAT
#pragma kernel ElementwisePowFloatFloat ELEMENTWISE POWFLOATFLOAT
#pragma kernel ScalarBroadcastPowFloatInt SCALARBROADCAST POWFLOATINT YINT
#pragma kernel BroadcastPowFloatInt BROADCAST POWFLOATINT YINT
#pragma kernel ElementwisePowFloatInt ELEMENTWISE POWFLOATINT YINT
#pragma kernel ScalarBroadcastAddFloat SCALARBROADCAST ADDFLOAT
#pragma kernel BroadcastAddFloat BROADCAST ADDFLOAT
#pragma kernel ElementwiseAddFloat ELEMENTWISE ADDFLOAT
#pragma kernel ScalarBroadcastSubFloat SCALARBROADCAST SUBFLOAT
#pragma kernel BroadcastSubFloat BROADCAST SUBFLOAT
#pragma kernel ElementwiseSubFloat ELEMENTWISE SUBFLOAT
#pragma kernel ScalarBroadcastMulFloat SCALARBROADCAST MULFLOAT
#pragma kernel BroadcastMulFloat BROADCAST MULFLOAT
#pragma kernel ElementwiseMulFloat ELEMENTWISE MULFLOAT
#pragma kernel ScalarBroadcastDivFloat SCALARBROADCAST DIVFLOAT
#pragma kernel BroadcastDivFloat BROADCAST DIVFLOAT
#pragma kernel ElementwiseDivFloat ELEMENTWISE DIVFLOAT
#pragma kernel ScalarBroadcastFloorDivFloat SCALARBROADCAST FLOORDIVFLOAT
#pragma kernel BroadcastFloorDivFloat BROADCAST FLOORDIVFLOAT
#pragma kernel ElementwiseFloorDivFloat ELEMENTWISE FLOORDIVFLOAT
#pragma kernel ScalarBroadcastTruncDivFloat SCALARBROADCAST TRUNCDIVFLOAT
#pragma kernel BroadcastTruncDivFloat BROADCAST TRUNCDIVFLOAT
#pragma kernel ElementwiseTruncDivFloat ELEMENTWISE TRUNCDIVFLOAT
#pragma kernel ScalarBroadcastMinFloat SCALARBROADCAST MINFLOAT
#pragma kernel BroadcastMinFloat BROADCAST MINFLOAT
#pragma kernel ElementwiseMinFloat ELEMENTWISE MINFLOAT
#pragma kernel ScalarBroadcastMaxFloat SCALARBROADCAST MAXFLOAT
#pragma kernel BroadcastMaxFloat BROADCAST MAXFLOAT
#pragma kernel ElementwiseMaxFloat ELEMENTWISE MAXFLOAT
#pragma kernel ScalarBroadcastMeanFloat SCALARBROADCAST MEANFLOAT
#pragma kernel BroadcastMeanFloat BROADCAST MEANFLOAT
#pragma kernel ElementwiseMeanFloat ELEMENTWISE MEANFLOAT
#pragma kernel ScalarBroadcastModFloat SCALARBROADCAST MODFLOAT
#pragma kernel BroadcastModFloat BROADCAST MODFLOAT
#pragma kernel ElementwiseModFloat ELEMENTWISE MODFLOAT
#pragma kernel ScalarBroadcastFModFloat SCALARBROADCAST FMODFLOAT
#pragma kernel BroadcastFModFloat BROADCAST FMODFLOAT
#pragma kernel ElementwiseFModFloat ELEMENTWISE FMODFLOAT
#pragma kernel ScalarBroadcastAtan2 SCALARBROADCAST ATAN2
#pragma kernel BroadcastAtan2 BROADCAST ATAN2
#pragma kernel ElementwiseAtan2 ELEMENTWISE ATAN2
#pragma kernel ScalarBroadcastPowIntFloat SCALARBROADCAST POWINTFLOAT XINT OINT
#pragma kernel BroadcastPowIntFloat BROADCAST POWINTFLOAT XINT OINT
#pragma kernel ElementwisePowIntFloat ELEMENTWISE POWINTFLOAT XINT OINT
#pragma kernel ScalarBroadcastPowIntInt SCALARBROADCAST POWINTINT XINT YINT OINT
#pragma kernel BroadcastPowIntInt BROADCAST POWINTINT XINT YINT OINT
#pragma kernel ElementwisePowIntInt ELEMENTWISE POWINTINT XINT YINT OINT
#pragma kernel ScalarBroadcastAddInt SCALARBROADCAST ADDINT XINT YINT OINT
#pragma kernel BroadcastAddInt BROADCAST ADDINT XINT YINT OINT
#pragma kernel ElementwiseAddInt ELEMENTWISE ADDINT XINT YINT OINT
#pragma kernel ScalarBroadcastSubInt SCALARBROADCAST SUBINT XINT YINT OINT
#pragma kernel BroadcastSubInt BROADCAST SUBINT XINT YINT OINT
#pragma kernel ElementwiseSubInt ELEMENTWISE SUBINT XINT YINT OINT
#pragma kernel ScalarBroadcastMulInt SCALARBROADCAST MULINT XINT YINT OINT
#pragma kernel BroadcastMulInt BROADCAST MULINT XINT YINT OINT
#pragma kernel ElementwiseMulInt ELEMENTWISE MULINT XINT YINT OINT
#pragma kernel ScalarBroadcastFloorDivInt SCALARBROADCAST FLOORDIVINT XINT YINT OINT
#pragma kernel BroadcastFloorDivInt BROADCAST FLOORDIVINT XINT YINT OINT
#pragma kernel ElementwiseFloorDivInt ELEMENTWISE FLOORDIVINT XINT YINT OINT
#pragma kernel ScalarBroadcastTruncDivInt SCALARBROADCAST TRUNCDIVINT XINT YINT OINT
#pragma kernel BroadcastTruncDivInt BROADCAST TRUNCDIVINT XINT YINT OINT
#pragma kernel ElementwiseTruncDivInt ELEMENTWISE TRUNCDIVINT XINT YINT OINT
#pragma kernel ScalarBroadcastMinInt SCALARBROADCAST MININT XINT YINT OINT
#pragma kernel BroadcastMinInt BROADCAST MININT XINT YINT OINT
#pragma kernel ElementwiseMinInt ELEMENTWISE MININT XINT YINT OINT
#pragma kernel ScalarBroadcastMaxInt SCALARBROADCAST MAXINT XINT YINT OINT
#pragma kernel BroadcastMaxInt BROADCAST MAXINT XINT YINT OINT
#pragma kernel ElementwiseMaxInt ELEMENTWISE MAXINT XINT YINT OINT
#pragma kernel ScalarBroadcastModInt SCALARBROADCAST MODINT XINT YINT OINT
#pragma kernel BroadcastModInt BROADCAST MODINT XINT YINT OINT
#pragma kernel ElementwiseModInt ELEMENTWISE MODINT XINT YINT OINT
#pragma kernel ScalarBroadcastFModInt SCALARBROADCAST FMODINT XINT YINT OINT
#pragma kernel BroadcastFModInt BROADCAST FMODINT XINT YINT OINT
#pragma kernel ElementwiseFModInt ELEMENTWISE FMODINT XINT YINT OINT
#pragma kernel ScalarBroadcastBitwiseAnd SCALARBROADCAST BITWISEAND XINT YINT OINT
#pragma kernel BroadcastBitwiseAnd BROADCAST BITWISEAND XINT YINT OINT
#pragma kernel ElementwiseBitwiseAnd ELEMENTWISE BITWISEAND XINT YINT OINT
#pragma kernel ScalarBroadcastBitwiseOr SCALARBROADCAST BITWISEOR XINT YINT OINT
#pragma kernel BroadcastBitwiseOr BROADCAST BITWISEOR XINT YINT OINT
#pragma kernel ElementwiseBitwiseOr ELEMENTWISE BITWISEOR XINT YINT OINT
#pragma kernel ScalarBroadcastBitwiseXor SCALARBROADCAST BITWISEXOR XINT YINT OINT
#pragma kernel BroadcastBitwiseXor BROADCAST BITWISEXOR XINT YINT OINT
#pragma kernel ElementwiseBitwiseXor ELEMENTWISE BITWISEXOR XINT YINT OINT

#pragma warning( disable : 3556 )
#include "Tensor.cginc"

uint shapeO[8];
uint stridesO[8];
uint shapeX[8];
uint stridesX[8];
uint shapeY[8];
uint stridesY[8];
uint2 unrolledDispatchArgs;
int rank;
uint MaxIndexO;
uint MaxBlockIndexX;

#ifdef SCALARMAD
float s;
float b;
#endif

#ifdef MEANFLOAT
float alpha;
float beta;
#endif

#ifdef XINT
StructuredBuffer<int> Xptr;
#else
StructuredBuffer<float> Xptr;
#endif
#ifdef YINT
StructuredBuffer<int> Bptr;
#else
StructuredBuffer<float> Bptr;
#endif
#ifdef OINT
RWStructuredBuffer<int> Optr;
#else
RWStructuredBuffer<float> Optr;
#endif


#ifdef PRELU
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastPRelu(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = ((1+y)*x+(1-y)*abs(x))*0.5f;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastPRelu(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = ((1+y)*x+(1-y)*abs(x))*0.5f;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwisePRelu(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = ((1+y)*x+(1-y)*abs(x))*0.5f;
}
#endif
#endif

#ifdef POWFLOATFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastPowFloatFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = SignedPow(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastPowFloatFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = SignedPow(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwisePowFloatFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = SignedPow(x, y);
}
#endif
#endif

#ifdef POWFLOATINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastPowFloatInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    float4 O = SignedPow(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastPowFloatInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = SignedPow(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwisePowFloatInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = SignedPow(x, y);
}
#endif
#endif

#ifdef ADDFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastAddFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = x + y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastAddFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = x + y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseAddFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = x + y;
}
#endif
#endif

#ifdef SUBFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastSubFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = x - y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastSubFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = x - y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseSubFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = x - y;
}
#endif
#endif

#ifdef MULFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastMulFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = x * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastMulFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = x * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseMulFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = x * y;
}
#endif
#endif

#ifdef DIVFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastDivFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = x / y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastDivFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = x / y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseDivFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = x / y;
}
#endif
#endif

#ifdef FLOORDIVFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastFloorDivFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = floor(x / y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastFloorDivFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = floor(x / y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseFloorDivFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = floor(x / y);
}
#endif
#endif

#ifdef TRUNCDIVFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastTruncDivFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = trunc(x / y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastTruncDivFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = trunc(x / y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseTruncDivFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = trunc(x / y);
}
#endif
#endif

#ifdef MINFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastMinFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = min(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastMinFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = min(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseMinFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = min(x, y);
}
#endif
#endif

#ifdef MAXFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastMaxFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = max(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastMaxFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = max(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseMaxFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = max(x, y);
}
#endif
#endif

#ifdef MEANFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastMeanFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = alpha * x + beta * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastMeanFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = alpha * x + beta * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseMeanFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = alpha * x + beta * y;
}
#endif
#endif

#ifdef MODFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastModFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = x - floor(x/y) * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastModFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = x - floor(x/y) * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseModFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = x - floor(x/y) * y;
}
#endif
#endif

#ifdef FMODFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastFModFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = fmod(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastFModFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = fmod(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseFModFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = fmod(x, y);
}
#endif
#endif

#ifdef ATAN2
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastAtan2(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    float4 O = atan2(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastAtan2(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    float4 x = float4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    float4 O = atan2(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseAtan2(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    float x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = atan2(x, y);
}
#endif
#endif

#ifdef POWINTFLOAT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastPowIntFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float y = Bptr[0];
    int4 O = SignedPow(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastPowIntFloat(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    float4 y = float4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = SignedPow(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwisePowIntFloat(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    float y = Bptr[indexY];
    Optr[threadIdx] = SignedPow(x, y);
}
#endif
#endif

#ifdef POWINTINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastPowIntInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = SignedPowInt(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastPowIntInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = SignedPowInt(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwisePowIntInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = SignedPowInt(x, y);
}
#endif
#endif

#ifdef ADDINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastAddInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x + y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastAddInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x + y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseAddInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x + y;
}
#endif
#endif

#ifdef SUBINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastSubInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x - y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastSubInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x - y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseSubInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x - y;
}
#endif
#endif

#ifdef MULINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastMulInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastMulInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x * y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseMulInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x * y;
}
#endif
#endif

#ifdef FLOORDIVINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastFloorDivInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = (x / y) - ((((x ^ y) >> 31) & 1) & ((((x % y) | -(x % y)) >> 31) & 1));

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastFloorDivInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = (x / y) - ((((x ^ y) >> 31) & 1) & ((((x % y) | -(x % y)) >> 31) & 1));

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseFloorDivInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = (x / y) - ((((x ^ y) >> 31) & 1) & ((((x % y) | -(x % y)) >> 31) & 1));
}
#endif
#endif

#ifdef TRUNCDIVINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastTruncDivInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x / y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastTruncDivInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x / y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseTruncDivInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x / y;
}
#endif
#endif

#ifdef MININT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastMinInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = min(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastMinInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = min(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseMinInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = min(x, y);
}
#endif
#endif

#ifdef MAXINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastMaxInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = max(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastMaxInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = max(x, y);

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseMaxInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = max(x, y);
}
#endif
#endif

#ifdef MODINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastModInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = ((x % y) + y) % y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastModInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = ((x % y) + y) % y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseModInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = ((x % y) + y) % y;
}
#endif
#endif

#ifdef FMODINT
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastFModInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x % y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastFModInt(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x % y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseFModInt(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x % y;
}
#endif
#endif

#ifdef BITWISEAND
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastBitwiseAnd(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x & y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastBitwiseAnd(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x & y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseBitwiseAnd(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x & y;
}
#endif
#endif

#ifdef BITWISEOR
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastBitwiseOr(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x | y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastBitwiseOr(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x | y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseBitwiseOr(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x | y;
}
#endif
#endif

#ifdef BITWISEXOR
#ifdef SCALARBROADCAST
[numthreads(64, 1, 1)]
void ScalarBroadcastBitwiseXor(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int y = Bptr[0];
    int4 O = x ^ y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef BROADCAST
[numthreads(64, 1, 1)]
void BroadcastBitwiseXor(uint3 groupThreadID : SV_GroupThreadID, uint3 groupID : SV_GroupID)
{
    uint4 idx = groupID.y * MaxBlockIndexX + (groupID.x << 8 | uint4(0,64,128,192) | groupThreadID.x);
    #if !defined(SHADER_API_D3D11)
    idx = min(idx, MaxIndexO);
    #endif

    int4 x = int4(Xptr[idx.x], Xptr[idx.y], Xptr[idx.z], Xptr[idx.w]);
    int4 y = int4(Bptr[idx.x], Bptr[idx.y], Bptr[idx.z], Bptr[idx.w]);
    int4 O = x ^ y;

    #if !defined(SHADER_API_D3D11)
    if (idx.x <= MaxIndexO)
        Optr[idx.x] = O.x;
    if (idx.y <= MaxIndexO)
        Optr[idx.y] = O.y;
    if (idx.z <= MaxIndexO)
        Optr[idx.z] = O.z;
    if (idx.w <= MaxIndexO)
        Optr[idx.w] = O.w;

    #else
    uint4 storeIndex = 0xFFFFFFFF;
    storeIndex.x = idx.x <= MaxIndexO ? idx.x : storeIndex.x;
    storeIndex.y = idx.y <= MaxIndexO ? idx.y : storeIndex.y;
    storeIndex.z = idx.z <= MaxIndexO ? idx.z : storeIndex.z;
    storeIndex.w = idx.w <= MaxIndexO ? idx.w : storeIndex.w;
    Optr[storeIndex.x] = O.x;
    Optr[storeIndex.y] = O.y;
    Optr[storeIndex.z] = O.z;
    Optr[storeIndex.w] = O.w;
    #endif
}
#endif

#ifdef ELEMENTWISE
[numthreads(64, 1, 1)]
void ElementwiseBitwiseXor(uint3 dispatchThreadID : SV_DispatchThreadID)
{
    uint threadIdx = unrolledDispatchArgs.x * dispatchThreadID.y + dispatchThreadID.x;
    if (threadIdx >= unrolledDispatchArgs.y)
        return;

    uint indexX = 0; uint indexY = 0;
    for (int axis = (SHAPE_MAXRANK - 1); axis > rank; axis--)
    {
        indexX += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeX[axis]) * stridesX[axis];
        indexY += (((threadIdx / stridesO[axis]) % shapeO[axis]) % shapeY[axis]) * stridesY[axis];
    }

    int x = Xptr[indexX];
    int y = Bptr[indexY];
    Optr[threadIdx] = x ^ y;
}
#endif
#endif
